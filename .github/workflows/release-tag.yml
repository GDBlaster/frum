name: Publish Release Image

on:
  release:
    types: [published]

jobs:
  publish-back:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push
        run: |
          TAG_NAME=${{ github.ref_name }}
          IMAGE_ID=ghcr.io/gdblaster/frum/back

          docker build -t $IMAGE_ID:$TAG_NAME -t $IMAGE_ID:latest ./back

          docker push $IMAGE_ID:$TAG_NAME
          docker push $IMAGE_ID:latest

  publish-front:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push
        run: |
          TAG_NAME=${{ github.ref_name }}
          IMAGE_ID=ghcr.io/gdblaster/frum/front

          docker build -t $IMAGE_ID:$TAG_NAME -t $IMAGE_ID:latest ./front

          docker push $IMAGE_ID:$TAG_NAME
          docker push $IMAGE_ID:latest

  upload-assets:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Interpolate image tags
        run: sed -i "s/__VERSION__/${{ github.ref_name }}/g" release/compose.yml

      - name: Zip release folder
        run: zip -r "release-${{ github.ref_name }}.zip" release/

      - name: Upload Zip to Release
        uses: softprops/action-gh-release@v2
        with:
          files: release-${{ github.ref_name }}.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
